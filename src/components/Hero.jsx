import React from "react";

const Hero = ({transition, onSky, onHome, value}) => {
  return (
    <section className="relative w-full h-screen mx-auto bg-transparent">
      
      {/* Not Really Using Much Anymore TBH */}
    </section>
  );
};

export default Hero;
